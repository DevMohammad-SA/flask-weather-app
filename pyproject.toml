[project]
name = "flask-weather-app"
version = "0.1.0"
description = "Flask-based web application that provides weather information for different locations"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["Flask>=3.1.0", "python-dotenv>=1.0.1", "requests>=2.32.3"]
